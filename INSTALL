Clone the source files.

$ git clone https://github.com/MasashiKiyomi/burrtools

Then, create build directory.

$ cd burrtools
$ mkdir build

Then cmake and make.

$ cd build
$ cmake ..
$ make

If you want to install the tools, do make install.

$ sudo make install
